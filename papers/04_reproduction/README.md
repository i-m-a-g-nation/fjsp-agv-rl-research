# 04_reproduction

本目录用于放后续复现评估材料。

当前建议：

1. Phase 1 完成并通过 `pytest` 前，不复现任何 RL/GNN 论文代码。
2. 候选复现对象先只记录，不安装依赖、不训练、不引入项目代码。
3. 后续每个候选仓库先写 `notes/reproduction_candidates.md`，确认 README、入口脚本、数据和 feasibility checker 对接方式。
